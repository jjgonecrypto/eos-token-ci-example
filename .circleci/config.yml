version: 2
jobs:
  build:
    docker:
      - image: eosio/cdt:v1.4.0
      - image: eosio/eos:v1.4.3
        commands:
          - "keosd --http-server-address=0.0.0.0:5555"
          - "nodeos --version || true"
    working_directory: ~

    steps:
      - checkout

      # - run:
      #     name: NODEOS version
      #     command: |
      #       nodeos --version

      # - run:
      #     name: KEOSD version
      #     command: |
      #       keosd --version

      - run:
          name: EOSIO-CPP version
          command: |
            eosio-cpp --version

            curl localhost:5555
